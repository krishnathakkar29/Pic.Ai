import Logout from "@/components/Auth/logout";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      page <Logout />
    </div>
  );
};

export default page;
